<%#
 Copyright 2019-Present the original author or authors from the JHipster project.

 This file is part of the JHipster project, see https://www.jhipster.tech/
 for more information.

 Licensed under the Apache License, Version 2.0 (the "License");
 you may not use this file except in compliance with the License.
 You may obtain a copy of the License at

      http://www.apache.org/licenses/LICENSE-2.0

 Unless required by applicable law or agreed to in writing, software
 distributed under the License is distributed on an "AS IS" BASIS,
 WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 See the License for the specific language governing permissions and
 limitations under the License.
-%>
export class <%= entityClass %>ComponentsPage {
    noResult = cy.getContainingText('ion-label', 'No <%= entityClassPluralHumanized %> found.');

    <%_ if (!readOnly) { _%>
    clickOnCreateButton() {
        cy.get('ion-fab-button').click();
    }
    <%_ } _%>

    clickOnLastViewButton() {
        cy.get('ion-item').last().click();
    }

    getTitle() {
        return cy.get('ion-title').get(2);
    }

    getEntitiesNumber() {
      return cy.get('page-<%= entityFileName %> ion-item').count();
    }
}

<%_ if (!readOnly) { _%>

export class <%= entityClass %>UpdatePage {
    pageTitle = cy.get('ion-title').get(3);
    saveButton = cy.get('ion-button').get(1);

<%_ fields.forEach((field) => {
    const fieldName = field.fieldName;
    const fieldType = field.fieldType;
    const fieldIsEnum = field.fieldIsEnum;
    const fieldTypeBlobContent = field.fieldTypeBlobContent;
_%>
    <%_ if (fieldType === 'Boolean') { _%>
    <%_ } else if (fieldIsEnum) { _%>
    <%= fieldName %>Select = cy.get('ion-select[formControlName="<%= fieldName %>"]');
    <%_ } else if (['byte[]', 'ByteBuffer'].includes(fieldType) && fieldTypeBlobContent === 'text') { _%>
    <%= fieldName %>Input = cy.get('ion-textarea[formControlName="<%= fieldName %>"] textarea');
    <%_ } else if (['byte[]', 'ByteBuffer'].includes(fieldType)) { _%>
    <%= fieldName %>Input = cy.get('ion-input[formControlName="<%= fieldName %>"] input');
    <%_ } else if (fieldType === 'LocalDate') { _%>
    <%_ } else if (['Instant', 'ZonedDateTime'].includes(fieldType)) { _%>
    <%_ } else { _%>
    <%= fieldName %>Input = cy.get('ion-input[formControlName="<%= fieldName %>"] input');
    <%_ } _%>
<%_ }); _%>


    getPageTitle() {
        return this.pageTitle;
    }

<%_ fields.forEach((field) => {
    const fieldName = field.fieldName;
    const fieldNameCapitalized = field.fieldNameCapitalized;
    const fieldNameHumanized = field.fieldNameHumanized;
    const fieldType = field.fieldType;
    const fieldTypeBlobContent = field.fieldTypeBlobContent;
    const fieldIsEnum = field.fieldIsEnum;
    let fieldInputType = 'text';
    let ngModelOption = '';
_%>
    <%_ if (fieldType === 'Boolean') { _%>
    <%_ } else if (fieldIsEnum) { _%>
    <%= fieldName %>SelectLastOption() {
        this.<%= fieldName %>Select.click();
        cy.get('alert-radio').last().click();
        cy.get('alert-button').last().click();
    }
    <%_ } else if (['byte[]', 'ByteBuffer'].includes(fieldType) && fieldTypeBlobContent === 'text') { _%>
    set<%= fieldNameCapitalized %>Input(<%= fieldName %>: string) {
        this.<%= fieldName %>Input.type(<%= fieldName %>);
    }
    <%_ } else if (['Instant', 'ZonedDateTime', 'LocalDate'].includes(fieldType)) { _%>
    <%_ } else { _%>
    set<%= fieldNameCapitalized %>Input(<%= fieldName %>: string) {
        this.<%= fieldName %>Input.type(<%= fieldName %>);
    }
    <%_ } _%>
<%_ }); _%>

    save() {
        this.saveButton.click();
    }
}

export class <%= entityClass %>DetailPage {
    pageTitle = cy.get('ion-title').get(3);
    deleteButton = cy.get('ion-button[color="danger"]');
    <%_ for (idx in fields) {
    const fieldName = fields[idx].fieldName;
    const fieldType = fields[idx].fieldType;
    const fieldTypeBlobContent = fields[idx].fieldTypeBlobContent;
    const fieldIsEnum = fields[idx].fieldIsEnum;
    const newIndex = parseInt(idx) + 1;
    _%>
    <%_ if (fieldType === 'Boolean') { _%>
    <%_ } else if (['Instant', 'ZonedDateTime', 'LocalDate'].includes(fieldType)) { _%>
    <%_ } else if (fieldIsEnum) { _%>
    <%_ } else if (['byte[]', 'ByteBuffer'].includes(fieldType) && fieldTypeBlobContent === 'text') { _%>
    <%= fieldName %>Input = cy.get('span').get(<%=newIndex%>);
    <%_ } else if (['byte[]', 'ByteBuffer'].includes(fieldType)) { _%>
    <%= fieldName %>Input = cy.get('span').get(<%=newIndex%>);
    <%_ } else { _%>
    <%= fieldName %>Input = cy.get('span').get(<%=newIndex%>);
    <%_ }; _%>

    <%_ }; _%>

    <%_ fields.forEach((field) => {
      const fieldName = field.fieldName;
      const fieldNameCapitalized = field.fieldNameCapitalized;
      const fieldNameHumanized = field.fieldNameHumanized;
      const fieldType = field.fieldType;
      const fieldTypeBlobContent = field.fieldTypeBlobContent;
      const fieldIsEnum = field.fieldIsEnum;
      let fieldInputType = 'text';
      let ngModelOption = '';
      _%>

    <%_ if (fieldType === 'Boolean') { _%>
    <%_ } else if (fieldIsEnum) { _%>

    <%_ } else if (['byte[]', 'ByteBuffer'].includes(fieldType) && fieldTypeBlobContent === 'text') { _%>
    get<%= fieldNameCapitalized %>Input() {
        return this.<%= fieldName %>Input;
    }
    <%_ } else if (['Instant', 'ZonedDateTime', 'LocalDate'].includes(fieldType)) { _%>
    <%_ } else { _%>
    get<%= fieldNameCapitalized %>Input() {
        return this.<%= fieldName %>Input;
    }
    <%_ } _%>
    <%_ }); _%>

    clickOnDeleteButton() {
      this.deleteButton.click();
    }

    getPageTitle() {
      this.pageTitle;
    }
}
<%_ } _%>
